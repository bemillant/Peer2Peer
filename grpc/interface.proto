syntax = "proto3";

package ping;
option go_package = "https://github.com/bemillant/Peer2Peer;ping";


service Ping {
    rpc passToken (PassToken)returns (Reply) {}
}

message Reply {
    string message = 1;
}

message PassToken {
    int32 skrrrtNumber = 1;
}